<% content_for(:title, "#{current_user.user_name} -  membership" )%>
<section id="user_porfile" class="container">
	<div class="container">
		<div class="row">
			<div class="col-md-3">
				<%= render "shared/user_Profile_Menu" %>
			</div>
			<div class="col-md-9">
				<h2>Membership Status:</h2>
				<hr>
				<h5>Your Status: <%= current_user.role %></h5>
				<br>
				<button class="btn btn-info btn-lg">Update your account</button>
			</div>
		</div>
	</div>
</section>
