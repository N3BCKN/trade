<% content_for(:title, "#{current_user.user_name} - Profile" )%>
<div class="text-center">
	<div class="container">
	<%= render "shared/user_Profile_Menu" %>
	<h2>Hello, <%= current_user.user_name %></h2>
	</div>
</div>
